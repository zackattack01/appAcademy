<%= note.words %>
Posted by 
<%= "#{note.user.email} #{time_ago_in_words(note.created_at)}" %>
<% if current_user = note.user %>
<%= link_to 'delete this note', track_note_url(note), method: :delete %>
<% end %>